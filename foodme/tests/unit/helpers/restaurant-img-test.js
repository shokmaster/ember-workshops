
import { restaurantImg } from 'foodme/helpers/restaurant-img';
import { module, test } from 'qunit';

module('Unit | Helper | restaurant img');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = restaurantImg([42]);
  assert.ok(result);
});

